{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug backend.app",
            "type": "debugpy",
            "request": "launch",
            "module": "backend.app",
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal"
        }
    ]
}